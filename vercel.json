{
  "version": 2,
  "installCommand": "python -m pip install -r backend/requirements.txt && npm install --prefix frontend",
  "buildCommand": "npm run build --prefix frontend && rm -rf dist && cp -r frontend/dist dist",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

