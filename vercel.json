{
  "version": 2,
  "installCommand": "if [ -f frontend/package.json ]; then npm install --prefix frontend; else npm install; fi",
  "buildCommand": "if [ -f frontend/package.json ]; then npm run build --prefix frontend && rm -rf dist && cp -r frontend/dist dist; else npm run build; fi",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

